name: Simple Lint
on: pull_request

jobs:
  run-linters:
    name: simple run for linters
    runs-on: ubuntu-latest

    steps:
      - name: Checkout git repo
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: yarn install

      - name: Run linters
        run: yarn lint
     