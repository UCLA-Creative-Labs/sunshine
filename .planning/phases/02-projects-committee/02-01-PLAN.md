---
phase: 02-projects-committee
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(portal)/portal/internal/projects/page.tsx
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03

must_haves:
  truths:
    - "Internal user sees all projects listed with name and quarter/year"
    - "Each project row is clickable and navigates to /portal/internal/projects/[projectId]"
    - "The row for the currently-selected project is highlighted blue when a [projectId] route is active"
    - "Empty state shows 'No projects found' when no projects exist"
  artifacts:
    - path: "src/app/(portal)/portal/internal/projects/page.tsx"
      provides: "Client component rendering full project list with highlight and navigation"
      min_lines: 50
  key_links:
    - from: "src/app/(portal)/portal/internal/projects/page.tsx"
      to: "src/lib/supabase/projectService.ts"
      via: "getAllProjects() called in useEffect"
      pattern: "getAllProjects"
    - from: "project row click"
      to: "/portal/internal/projects/[projectId]"
      via: "router.push or Link href"
      pattern: "projectId"
---

<objective>
Replace the placeholder Projects committee page with a real project list view.

Purpose: Give internal users a browsable list of all projects (PROJ-01) with navigation into member management (PROJ-02) and active-row highlighting (PROJ-03).
Output: A fully functional client component at `/portal/internal/projects` that fetches and displays all projects.
</objective>

<execution_context>
@/Users/bryanzhang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bryanzhang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-projects-committee/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/project.ts:
```typescript
export interface Project {
    id: number;          // used as route param (convert to string for URL)
    year: string;        // e.g. "2024-2025"
    quarter: string;     // e.g. "Fall"
    projectName: string;
    // (other fields not needed for list view)
}
```

From src/lib/supabase/projectService.ts:
```typescript
export async function getAllProjects(): Promise<Project[]>
// Returns all projects from DB. Empty array on error.
```

From src/components/portal/internal/InternalSidebar.tsx:
```typescript
// NAV_STYLES active class for selected row highlight:
const NAV_STYLES = {
  active: "bg-[rgba(108,162,255,0.25)] text-[#3F86FF]",
  inactive: "text-gray-600 hover:bg-gray-100",
}
// Card style from MyProjectContent: "rounded-2xl border border-[#D4D7E5] bg-white p-6 shadow-lg"
```

From src/app/(portal)/portal/internal/layout.tsx:
```typescript
// Layout provides: <div className="mx-auto max-w-5xl space-y-6">{children}</div>
// No additional wrapper needed — just render content directly.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project list page — full implementation</name>
  <files>src/app/(portal)/portal/internal/projects/page.tsx</files>
  <action>
Replace the existing placeholder with a real 'use client' component. The component should:

1. On mount, call `getAllProjects()` from `src/lib/supabase/projectService.ts` and store in state. Show a spinner (`animate-spin rounded-full h-8 w-8 border-b-2 border-blue-500`) during load.

2. Render a page heading "Projects" in `text-2xl font-bold text-gray-900` and below it a card container (`rounded-2xl border border-[#D4D7E5] bg-white shadow-lg`).

3. Inside the card, render each project as a clickable row that uses `router.push` (or a Next.js `<Link>`) to navigate to `/portal/internal/projects/${project.id}`.
   - Each row displays: project name (left, `font-semibold`) and quarter/year label (right, `text-sm text-gray-500`), formatted as `${project.quarter} ${project.year}` (e.g., "Fall 2024-2025").
   - Row padding: `px-4 py-3`. Add a bottom divider between rows (`border-b border-[#D4D7E5]`) except on the last row.

4. Active row highlight: use `usePathname()` from `next/navigation`. A row is active when `pathname` starts with `/portal/internal/projects/${project.id}`. Apply active row classes `bg-[rgba(108,162,255,0.25)] text-[#3F86FF]` to active rows; inactive rows get `hover:bg-gray-50 cursor-pointer`.

5. Empty state: if `projects.length === 0` after loading, show `<p className="text-gray-500 text-sm px-4 py-6">No projects found</p>`.

6. Do NOT add search/filter — user deferred this.
  </action>
  <verify>
    <automated>cd /Users/bryanzhang/Documents/GitHub/sunshine && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>
    `yarn build` passes with no TypeScript or compile errors.
    Visiting `/portal/internal/projects` (as an internal user) shows a project list with project names and quarter/year. Clicking a row navigates to `/portal/internal/projects/[id]`. Active row (when on a project route) is highlighted blue.
  </done>
</task>

</tasks>

<verification>
1. `yarn build` completes without errors.
2. File `src/app/(portal)/portal/internal/projects/page.tsx` is a 'use client' component using `getAllProjects`, `usePathname`, and `useRouter`/Link.
3. The component handles loading state, empty state, and populated list.
</verification>

<success_criteria>
- Project list renders from real DB data (no hardcoded content)
- Each project row shows `${quarter} ${year}` as the secondary label
- Clicking a project navigates to `/portal/internal/projects/[projectId]`
- Active project row (pathname match) is highlighted with `bg-[rgba(108,162,255,0.25)] text-[#3F86FF]`
- `yarn build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-projects-committee/02-01-SUMMARY.md` following the summary template.
</output>
